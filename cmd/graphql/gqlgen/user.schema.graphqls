extend type Query {
  getUserById(id: ID!): User!
}

extend type Mutation {
  createUser(input: CreateUserInput!): ID!
}

type User {
  id: ID!
  name: String!
  investorProfile: Int!
}

input CreateUserInput {
  name: String!
  investorProfile: Int!
}
